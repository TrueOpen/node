# Private address value storage

These collections store canonical account address bytes in private Node messages while retaining their public wire messages at Query and Genesis boundaries. Every collection keeps its existing key encoding. Changing a Bech32 address key to raw bytes requires a separate ordering and pagination review.

| Collection | Public counterpart and field mapping | Private-only differences | Projection and Genesis round trip |
|---|---|---|---|
| `earnings` | `hub.v1.EarningsState`: `address` maps between Bech32 and 20 codec bytes; each `Amount.atomic_units`, version, and height map without changing their values. | `EarningsStoreState.address` is raw bytes. Amount wrappers are flattened to their atomic-unit strings. No additional account fact is stored. | `earningsStateToStore` and `earningsStorePublicProjection` in `x/hub/keeper/earnings_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportEarningsGenesis` returns the public row and checks key/address agreement. `TestEarningsAddressValueAndGenesisRoundTrip` verifies the public export, reimport, and raw stored address. |
| `builder_admission` | `hub.v1.BuilderAdmissionState`: `builder_address` maps between Bech32 and 20 codec bytes; status, set version, proposal identifier, and height retain their values. | `BuilderAdmissionStoreState.builder_address` is raw bytes; `status` stores the closed public enum number as `int32`; `has_source_proposal_id` preserves public optional-field presence. | `builderAdmissionStateToStore` and `builderAdmissionStorePublicProjection` in `x/hub/keeper/builder_admission_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportBuilderAdmissionGenesis` returns the public row and checks key/address agreement. `TestBuilderAdmissionGenesisRoundTrip` verifies export and reimport; `TestBuilderAdmissionStoresRawAddress` verifies the stored representation. |
| `validator_bridge_signer` | `hub.v1.ValidatorBridgeSignerState`: `operator_address` maps between Bech32 and 20 codec bytes; the bridge signer bytes, version, possession proof, and height retain their values. | `ValidatorBridgeSignerStoreState.operator_address` is raw bytes. The Bech32 collection key is unchanged. | `validatorBridgeSignerStateToStore` and `validatorBridgeSignerStorePublicProjection` in `x/hub/keeper/bridge_treasury_address_store.go` are the conversion points. `InitGenesis` rebuilds the row; `exportValidatorBridgeSigners` returns public rows and checks key/address agreement. `TestBridgeGenesisSurvivesExportImportRoundTrip` verifies raw storage and the public round trip. |
| `treasury_spend_recipient_epoch` | `hub.v1.TreasurySpendRecipientEpochState`: `recipient_address` maps between Bech32 and 20 codec bytes; epoch and spent amount retain their values. | `TreasurySpendRecipientEpochStoreState.recipient_address` is raw bytes and the public amount wrapper is flattened to its atomic-unit string. Its existing composite key already contains raw address bytes and is unchanged. | `treasuryRecipientStateToStore` and `treasuryRecipientStorePublicProjection` in `x/hub/keeper/bridge_treasury_address_store.go` are the conversion points. `InitGenesis` rebuilds the row; `exportTreasuryRecipientEpochs` returns public rows and checks key/address agreement. `TestTreasuryCleanupCursorGenesisRoundTrip` verifies raw storage and the public round trip. |
| `builder_set` | `hub.v1.BuilderSetState`: each `active_builders` member maps between Bech32 and 20 codec bytes; all other fields retain their values and presence. | `BuilderSetStoreState.active_builders` is repeated raw bytes; explicit presence flags retain the three optional heights and proposal identifier fields. | `builderSetStateToStore` and `builderSetStorePublicProjection` in `x/hub/keeper/builder_set_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportBuilderSets` returns the public rows. `TestBuilderSetValuesStoreRawMemberAddresses` checks stored members and the existing replacement tests exercise Genesis and pruning. |
| `pending_builder_set_replacement` | `hub.v1.BuilderSetPendingReplacementState`: each `next_active_builders` member maps between Bech32 and 20 codec bytes; other fields retain their values. | `BuilderSetPendingReplacementStoreState.next_active_builders` is repeated raw bytes. | `pendingBuilderSetStateToStore` and `pendingBuilderSetStorePublicProjection` in `x/hub/keeper/builder_set_store.go` are the conversion points. `InitGenesis` rebuilds the private singleton; `GetPendingBuilderSetReplacement` projects it for public export. `TestBuilderSetValuesStoreRawMemberAddresses` checks stored members and the replacement tests exercise the public Genesis surface. |
| `task_builder_selection` | `task.v1.TaskBuilderSelectionState`: each `selected_task_builders` member maps between Bech32 and 20 codec bytes; every other field retains its value. The selected rank order is preserved exactly. | `TaskBuilderSelectionStoreState.selected_task_builders` is repeated raw bytes. Its Hash32 task key is unchanged. | `taskBuilderSelectionToStore` and `ProjectTaskBuilderSelection` in `x/task/keeper/task_builder_selection_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportTaskBuilderSelections` returns the public rows. `TestGenesisV03RoundTripRebuildsDerivedIndexes` verifies raw storage and public round trip; Task Query and app invariants consume the same projection. |
| `treasury_spend_receipt` | `hub.v1.TreasurySpendReceiptState`: `recipient` maps between Bech32 and 20 codec bytes; proposal, item, heights, digest, amount, epoch, and purpose retain their values. | `TreasurySpendReceiptStoreState.recipient` is raw bytes; the amount wrapper is flattened to its atomic-unit string and the closed purpose enum retains its number. | `treasurySpendReceiptToStore` and `treasurySpendReceiptStorePublicProjection` in `x/hub/keeper/treasury_receipt_cursor_store.go` are the conversion points. Genesis imports the private row and `exportTreasurySpendReceipts` returns public rows. `TestTreasurySpendReceiptGenesisRoundTripAndTamperRejection` verifies raw storage and public round trip. |
| `treasury_spend_epoch_cleanup_cursor` | `hub.v1.TreasurySpendEpochCleanupCursorState`: optional `last_recipient_address` maps between Bech32 and 20 codec bytes; counters and heights retain their values. | `TreasurySpendEpochCleanupCursorStoreState.last_recipient_address` is raw bytes; `has_last_recipient_address` preserves optional presence. | `treasuryCleanupCursorToStore` and `treasuryCleanupCursorStorePublicProjection` in `x/hub/keeper/treasury_receipt_cursor_store.go` are the conversion points. Genesis imports the private row and `exportTreasuryCleanupCursors` returns public rows. `TestTreasuryCleanupCursorGenesisRoundTrip` verifies raw storage and public round trip. |
| `vrf_key` | `hub.v1.VrfKeyState`: `operator_address` maps between Bech32 and 20 codec bytes; active and pending key facts retain their values. | `VrfKeyStoreState.operator_address` is raw bytes; two presence flags retain optional pending key and epoch fields. The Bech32 collection key is unchanged. | `vrfKeyStateToStore` and `vrfKeyStorePublicProjection` in `x/hub/keeper/vrf_key_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportVrfKeys` returns public rows. `TestVrfKeyGenesisRoundTrips` checks raw storage and public export/import. |
| `vrf_key_history` | `hub.v1.VrfKeyHistoryState`: `operator_address` maps between Bech32 and 20 codec bytes; effective and retired epochs and the public key retain their values. | `VrfKeyHistoryStoreState.operator_address` is raw bytes. The composite key retains its Bech32 address component. | `vrfKeyHistoryToStore` and `vrfKeyHistoryStorePublicProjection` in `x/hub/keeper/vrf_key_store.go` are the conversion points. `InitGenesis` rebuilds the private row; `exportVrfKeyHistory` returns public rows. The VRF activation and Genesis tests cover retained history and raw storage. |
| `cortex_node` | `hub.v1.CortexNodeState`: `operator_address` and optional `current_service_address` map between Bech32 and 20 codec bytes; all other identity and liability fields retain their values. | `CortexNodeStoreState` keeps both addresses as raw bytes, with an empty service address for an unbound identity. Its Bech32 key is unchanged. | `cortexNodeStateToStore` and `ProjectCortexNodeStore` in `x/hub/keeper/participant_address_store.go` are the conversion points. `InitGenesis` rebuilds the private row and `exportCortexNodes` returns public rows. `TestParticipantAddressValuesUseCodecBytes` verifies raw storage; service identity Genesis tests verify the public round trip. |
| `builder_state` | `hub.v1.BuilderState`: `builder_address` and optional `current_service_address` map between Bech32 and 20 codec bytes; other fields retain their values. | `BuilderStoreState` keeps both addresses as raw bytes. Its Bech32 key is unchanged. | `builderStateToStore` and `ProjectBuilderStore` in `x/hub/keeper/participant_address_store.go` are the conversion points. `InitGenesis` rebuilds the private row and `exportBuilders` returns public rows. `TestParticipantAddressValuesUseCodecBytes` verifies raw storage; Builder-set Genesis tests verify the public round trip. |
| `current_service_address` | `hub.v1.CurrentServiceAddressIndexState`: `operator_address` maps between Bech32 and 20 codec bytes; authorization nonce is unchanged. | `CurrentServiceAddressIndexStoreState.operator_address` is raw bytes. Its existing reverse-index key is unchanged and rebuilt from participant primaries on Genesis import. | `serviceAddressIndexToStore` and `ProjectCurrentServiceAddressIndexStore` in `x/hub/keeper/participant_address_store.go` are the conversion points. The index is derived rather than exported; Genesis reconstructs it from public Cortex and Builder rows. `TestParticipantAddressValuesUseCodecBytes` verifies raw storage, and service identity invariants check the rebuilt mapping. |
| `service_descriptor` | `hub.v1.ServiceDescriptorState`: `operator_address` maps between Bech32 and 20 codec bytes; participant kind, version, ordered endpoints, descriptor hash, and height retain their values. | `ServiceDescriptorStoreState.operator_address` is raw bytes. Nested `ServiceEndpointStoreV1` preserves optional TLS hash presence without storing the public endpoint message. Its existing participant key is unchanged. | `serviceDescriptorToStore` and `ProjectServiceDescriptorStore` in `x/hub/keeper/service_descriptor_store.go` are the conversion points. Genesis imports the private row and `exportServiceDescriptors` returns public rows. `TestParticipantAddressValuesUseCodecBytes` verifies raw storage; service identity Genesis tests cover the public round trip. |
| `candidate_slot_current` | `hub.v1.CandidateSlotCurrentState`: nonempty `operator_address` maps between Bech32 and 20 codec bytes; FREE slots retain an empty operator. | `CandidateSlotCurrentStoreState.operator_address` is raw bytes or empty. The numeric slot key is unchanged. | `candidateSlotCurrentToStore` and `ProjectCandidateSlotCurrentStore` in `x/hub/keeper/candidate_address_store.go` are the conversion points. Genesis imports the private row and `exportCandidateSlotCurrents` returns public rows; CandidatePool Genesis tests cover the round trip. |
| `candidate_slot_binding` | `hub.v1.CandidateSlotBindingState`: `operator_address` maps between Bech32 and 20 codec bytes; slot version, epochs, ref count, and binding hash retain their values. | `CandidateSlotBindingStoreState.operator_address` is raw bytes. The slot/version key is unchanged. | `candidateSlotBindingToStore` and `ProjectCandidateSlotBindingStore` in `x/hub/keeper/candidate_address_store.go` are the conversion points. Genesis imports the private row and `exportCandidateSlotBindings` returns public rows; CandidatePool Genesis tests cover the round trip. |
| `operator_candidate_slot` | `hub.v1.OperatorCandidateSlotState`: `operator_address` maps between Bech32 and 20 codec bytes; slot and version retain their values. | `OperatorCandidateSlotStoreState.operator_address` is raw bytes. The Bech32 reverse-index key is unchanged. | `operatorCandidateSlotToStore` and `ProjectOperatorCandidateSlotStore` in `x/hub/keeper/candidate_address_store.go` are the conversion points. This derived index is rebuilt from public slot rows at Genesis import and checked in both directions; it is not exported. |
| `candidate_pool_member` | `hub.v1.CandidatePoolMemberState`: `operator_address` maps between Bech32 and 20 codec bytes; epoch, slot, version, and binding hash retain their values. | `CandidatePoolMemberStoreState.operator_address` is raw bytes. The epoch/slot key is unchanged. | `candidatePoolMemberToStore` and `ProjectCandidatePoolMemberStore` in `x/hub/keeper/candidate_address_store.go` are the conversion points. Genesis imports the private row and `exportCandidatePoolMembers` returns public rows; CandidatePool Genesis tests cover the round trip. |
| `session_nonce` | `task.v1.SessionNonceState`: `user_address` maps between Bech32 and 20 codec bytes; the next nonce is unchanged. | `SessionNonceStoreState.user_address` is raw bytes; its Bech32 key is unchanged. | `sessionNonceToStore` and `ProjectSessionNonceStore` in `x/task/keeper/session_address_store.go` are the conversion points. Genesis imports private rows and `exportSessionNonces` returns public rows with key/address agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public export/import. |
| `stream_state` | `task.v1.StreamState`: `owner_user_address` maps between Bech32 and 20 codec bytes; session ID, counters, height, and status retain their values. | `StreamStoreState.owner_user_address` is raw bytes and the closed status enum retains its number. Its Hash32 session key is unchanged. | `streamToStore` and `ProjectStreamStore` in `x/task/keeper/session_address_store.go` are the conversion points. Genesis imports private rows and `exportStreams` returns public rows with key/session agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public export/import. |
| `session_terminal_summary` | `task.v1.SessionTerminalSummaryState`: `owner_user_address` maps between Bech32 and 20 codec bytes; sequence counts, root, and heights retain their values. | `SessionTerminalSummaryStoreState.owner_user_address` is raw bytes. Its Hash32 session key is unchanged. | `terminalSummaryToStore` and `ProjectTerminalSummaryStore` in `x/task/keeper/session_address_store.go` are the conversion points. Genesis imports private rows and `exportSessionTerminalSummaries` returns public rows with key/session agreement. `TestSessionTerminalSummaryGenesisStoresRawOwnerAndRoundTrips` checks raw storage and public export/import. |
| `verifier_payout` | `task.v1.VerifierPayoutState`: `operator_address` maps between Bech32 and 20 codec bytes; task and settlement identifiers, selected index, and amounts retain their values. | `VerifierPayoutStoreState.operator_address` is raw bytes and the three public amount wrappers are flattened to atomic-unit strings. Its task/index key is unchanged. | `verifierPayoutToStore` and `ProjectVerifierPayoutStore` in `x/task/keeper/verifier_payout_store.go` are the conversion points. Genesis imports private rows and `exportVerifierPayouts` returns public rows with key/value agreement. `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |
| `task_gas_reimbursement` | `task.v1.TaskGasReimbursementV1`: `fee_payer` maps between Bech32 and 20 codec bytes; task/transaction identifiers, item index, kind, gas basis, fee policy, height, and amounts retain their values. | `TaskGasReimbursementStoreState.fee_payer` is raw bytes; the three public amount wrappers are flattened to atomic-unit strings and the closed kind enum retains its number. Its task/transaction/item key is unchanged. | `gasReimbursementToStore` and `ProjectGasReimbursementStore` in `x/task/keeper/gas_reimbursement_store.go` are the conversion points. Genesis imports private rows and `exportGasReimbursements` returns public rows with key/value agreement. `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |
| `task_candidate_fact` | `task.v1.TaskCandidateFactState`: `operator_address` maps between Bech32 and 20 codec bytes; frozen candidate facts and ordering retain their values. | `TaskCandidateFactStoreState.operator_address` is raw bytes, four amount wrappers are flattened to atomic-unit strings, and stage/duty enums retain their numbers. Its task/stage/slot key is unchanged. | `candidateFactToStore` and `ProjectTaskCandidateFactStore` in `x/task/keeper/candidate_stage_store.go` are the conversion points. Genesis imports private rows and `exportTaskCandidateFacts` returns public rows with key/value agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public Genesis export/import. |
| `builder_stage_proposal` | `task.v1.BuilderStageProposalState`: `proposer_operator` maps between Bech32 and 20 codec bytes; proposal identity, stage, height, count, and attestation retain their values. | `BuilderStageProposalStoreState.proposer_operator` is raw bytes and the stage enum retains its number. Its task/stage/digest key is unchanged. | `builderStageProposalToStore` and `ProjectBuilderStageProposalStore` in `x/task/keeper/candidate_stage_store.go` are the conversion points. Genesis imports private rows and `exportBuilderStageProposals` returns public rows with key/value agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public Genesis export/import. |
| `data_unavailable_report` | `task.v1.DataUnavailableReportState`: `verifier_operator_address` maps between Bech32 and 20 codec bytes; round, bitmap, authorization nonce, height, and digest retain their values. | `DataUnavailableReportStoreState.verifier_operator_address` is raw bytes. Its task/round/Bech32-operator key is unchanged. | `dataUnavailableReportToStore` and `ProjectDataUnavailableReportStore` in `x/task/keeper/data_unavailable_report_store.go` are the conversion points. Genesis imports private rows and `exportDataUnavailableReports` returns public rows with key/value agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public Genesis export/import. |
| `builder_data_unavailable_aggregate` | `task.v1.BuilderDataUnavailableAggregateState`: `builder_operator_address` maps between Bech32 and 20 codec bytes; counts, hash, height, status, and ordered verifier slots retain their values. | `BuilderDataUnavailableAggregateStoreState.builder_operator_address` is raw bytes. Each private slot has an explicit presence flag for its optional report digest; the closed status enum retains its number. Its task/round/Bech32-operator key is unchanged. | `dataUnavailableAggregateToStore` and `ProjectDataUnavailableAggregateStore` in `x/task/keeper/data_unavailable_aggregate_store.go` are the conversion points. Genesis imports private rows and `exportDataUnavailableAggregates` returns public rows with key/value agreement. `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage, optional-slot presence, and public Genesis export/import. |
| `worker_evidence_receipt` | `task.v1.WorkerEvidenceReceiptState`: `worker_operator_address` maps between Bech32 and 20 codec bytes; task, evidence identity, hashes, sequence, and height retain their values. | `WorkerEvidenceReceiptStoreState.worker_operator_address` is raw bytes and the evidence-kind enum retains its number. Its task/Bech32-operator/kind/sequence key is unchanged. | `workerEvidenceReceiptToStore` and `ProjectWorkerEvidenceReceiptStore` in `x/task/keeper/worker_evidence_receipt_store.go` are the conversion points. Genesis imports private rows and `exportWorkerEvidenceReceipts` returns public rows with key/value agreement. `TestGenesisV03RebuildsVerifierWindowIndexes` checks raw storage and public Genesis export/import; app invariants consume the same projection. |
| `verifier_candidate_window_member` | `task.v1.VerifierCandidateWindowMemberState`: `operator_address` maps between Bech32 and 20 codec bytes; task, round, rank, slot, version, and rank hash retain their values. | `VerifierWindowMemberStoreState.operator_address` is raw bytes. Its task/round/rank key is unchanged. | `verifierWindowMemberToStore` and `ProjectVerifierWindowMemberStore` in `x/task/keeper/verifier_window_member_store.go` are the conversion points. Genesis imports private rows and `exportVerifierWindowMembers` returns public rows with key/value agreement. `TestVerifierWindowMemberAddressStoresRawBytesAndRoundTrips` checks raw storage and public Genesis export/import. |
| `commit_state` | `task.v1.CommitState`: `verifier_operator_address` maps between Bech32 and 20 codec bytes; commitment identity, digests, height, and status retain their values. | `CommitStoreState.verifier_operator_address` is raw bytes and the closed status enum retains its number. Its Hash32 commitment key is unchanged. | `commitToStore` and `ProjectCommitStore` in `x/task/keeper/commit_store.go` are the conversion points. Genesis imports private rows and `exportCommits` returns public rows with key/value agreement. `TestVerificationReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |
| `result_receipt_state` | `task.v1.ResultReceiptState`: `verifier_operator_address` maps between Bech32 and 20 codec bytes; result commitments and the metric summary retain their values. | `ResultReceiptStoreState.verifier_operator_address` is raw bytes. A private metric summary uses explicit flags to preserve both optional metric presences, including present-zero values. Its Hash32 commitment key is unchanged. | `resultReceiptToStore` and `ProjectResultReceiptStore` in `x/task/keeper/result_receipt_store.go` are the conversion points. Genesis imports private rows and `exportResultReceipts` returns public rows with key/value agreement. `TestVerificationReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage, optional presence, and public Genesis export/import. |
| `infer_receipt` | `task.v1.InferReceiptState`: `winner_worker` maps between Bech32 and 20 codec bytes; output facts, digests, heights, and the ordered evidence commitments retain their values. | `InferReceiptStoreState.winner_worker` is raw bytes. Each nested private evidence commitment retains kind, hash, and encoded size without storing a public message. Its Hash32 task key is unchanged. | `inferReceiptToStore` and `ProjectInferReceiptStore` in `x/task/keeper/infer_receipt_store.go` are the conversion points. Genesis imports private rows and `exportInferReceipts` returns public rows with key/value agreement. `TestGenesisV03RebuildsVerifierWindowIndexes` checks raw storage and public Genesis export/import; `TestInferReceiptPrivateProjectionPreservesEvidenceOrder` checks the nested order. |
| `verifier_assignment` | `task.v1.VerifierAssignmentState`: each ordered `selected_verifiers.operator_address` maps between Bech32 and 20 codec bytes; selection hashes, clocks, slots, and versions retain their values. | `VerifierAssignmentStoreState` holds private selected-verifier messages with raw address bytes. Its task/round key is unchanged. | `verifierAssignmentToStore` and `ProjectVerifierAssignmentStore` in `x/task/keeper/verifier_assignment_store.go` are the conversion points. Genesis imports private rows and `exportVerifierAssignments` returns public rows with key/value agreement. `TestVerifierWindowMemberAddressStoresRawBytesAndRoundTrips` checks stored member order and public Genesis export/import; app invariants use the same projection. |
| `settlement_facts_retained` | `task.v1.SettlementFactsRetainedState`: `settlement_duty_builder_operator` maps between Bech32 and 20 codec bytes; audit hashes, verdict, failure class, heights, and policy identifiers retain their values. | `SettlementFactsStoreState.settlement_duty_builder_operator` is raw bytes and the closed enums retain their numbers. Its Hash32 task key is unchanged. | `settlementFactsToStore` and `ProjectSettlementFactsStore` in `x/task/keeper/settlement_facts_store.go` are the conversion points. Genesis imports private rows and `exportSettlementFacts` returns public rows with key/value agreement. `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |
| `round_funding` | `task.v1.RoundFundingState`: `opener_address` maps between Bech32 and 20 codec bytes; custody amounts, policy snapshots, hash, outcome, and close height retain their values. | `RoundFundingStoreState.opener_address` is raw bytes; eleven amount wrappers are flattened to atomic-unit strings and three explicit flags retain optional-field presence. Its task/round key is unchanged. | `roundFundingToStore` and `ProjectRoundFundingStore` in `x/task/keeper/round_funding_store.go` are the conversion points. Genesis imports private rows and `exportRoundFundings` returns public rows with key/value agreement. `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage, optional presence, and public Genesis export/import. |
| `verification_round` | `task.v1.VerificationRoundState`: optional `opener_address` maps between Bech32 and 20 codec bytes; the other fields retain identical field numbers and wire types. | `VerificationRoundStoreState.opener_address` is optional raw bytes. Same-tag protobuf transcoding preserves all other optional presences; the task/round key is unchanged. | `verificationRoundToStore` and `ProjectVerificationRoundStore` in `x/task/keeper/verification_round_store.go` are the conversion points. `TestVerificationRoundPrivateStoreWireLayoutMatchesPublic` checks schema parity, `TestVerificationRoundPrivateProjectionPreservesOptionalFields` checks every optional field, and `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |
| `task_settlement` | `task.v1.TaskSettlementState`: optional `worker_operator_address` maps between Bech32 and 20 codec bytes; outcome, economic counts, hashes, heights, and amounts retain their values. | `TaskSettlementStoreState.worker_operator_address` is raw bytes with an explicit presence flag; eight amount wrappers are flattened to atomic-unit strings. Its Hash32 task key is unchanged. | `taskSettlementToStore` and `ProjectTaskSettlementStore` in `x/task/keeper/task_settlement_store.go` are the conversion points. Genesis imports private rows and `exportTaskSettlements` returns public rows with key/value agreement. `TestSettlementReceiptAddressesStoreRawBytesAndRoundTrip` checks raw storage, presence, and public Genesis export/import. |
| `task_terminal_summary` | `task.v1.TaskTerminalSummaryState`: optional `winner_worker` maps between Bech32 and 20 codec bytes; all retained task and settlement facts keep their field numbers and wire types. | `TaskTerminalSummaryStoreState.winner_worker` is optional raw bytes. Same-tag protobuf transcoding preserves `round2_outcome` presence; its Hash32 task key is unchanged. | `taskTerminalSummaryToStore` and `ProjectTaskTerminalSummaryStore` in `x/task/keeper/task_terminal_summary_store.go` are the conversion points. `TestTaskTerminalSummaryPrivateStoreWireLayoutMatchesPublic` checks schema parity; `TestTaskTerminalSummaryAddressStoresRawBytesAndRoundTrips` checks raw storage and public Genesis export/import. |
| `task_assignment` | `task.v1.TaskAssignmentState`: `winner_worker` maps between Bech32 and 20 codec bytes when present; the unset pre-assignment value stays empty. All other fields retain identical field numbers and wire types. | `TaskAssignmentStoreState.winner_worker` is raw bytes or empty. Same-tag protobuf transcoding preserves the other assignment facts; its Hash32 task key is unchanged. | `taskAssignmentToStore` and `ProjectTaskAssignmentStore` in `x/task/keeper/task_assignment_store.go` are the conversion points. `TestTaskAssignmentPrivateStoreWireLayoutMatchesPublic` checks schema parity and `TestGenesisV03RoundTripRebuildsDerivedIndexes` checks raw storage and public Genesis export/import. |
| `service_bond` | `hub.v1.ServiceBondState`: `operator_address` maps between Bech32 and 20 codec bytes; bond amounts, status, counters, and heights retain their values. | `ServiceBondStoreState.operator_address` is raw bytes. Its Bech32 key and effective-epoch index are unchanged. | `serviceBondToStore` and `ProjectServiceBondStore` in `x/hub/keeper/service_bond_address_store.go` are the conversion points. Genesis imports private rows and `exportServiceBonds` returns public rows with key/address agreement. `TestModelProfileGenesisRebuildsCanonicalRegistration` checks raw storage and public Genesis export/import. |
| `unbonding` | `hub.v1.UnbondingState`: `operator_address` maps between Bech32 and 20 codec bytes; identifier, amount, heights, status, and applied slash retain their values. | `UnbondingStoreState.operator_address` is raw bytes. Its Bech32/Hash32 key and derived indexes are unchanged. | `unbondingToStore` and `ProjectUnbondingStore` in `x/hub/keeper/service_bond_address_store.go` are the conversion points. Genesis imports private rows and `exportUnbondings` returns public rows with key/value agreement. `TestModelProfileGenesisRebuildsCanonicalRegistration` checks raw storage and public Genesis export/import. |
| `unbonding_receipt` | `hub.v1.UnbondingReceiptState`: `operator_address` maps between Bech32 and 20 codec bytes; custody amounts, terminal status, height, and receipt hash retain their values. | `UnbondingReceiptStoreState.operator_address` is raw bytes. Its Hash32 unbonding key is unchanged. | `unbondingReceiptToStore` and `ProjectUnbondingReceiptStore` in `x/hub/keeper/service_bond_address_store.go` are the conversion points. Genesis imports private rows after checking the chain-bound receipt hash, and `exportUnbondingReceipts` returns public rows. `TestServiceSlashWaterfallMovesCustodyWritesSummaryAndTerminatesUnbonding` checks raw storage and public Genesis export/import. |
| `task_liability_reservation` | `hub.v1.TaskLiabilityReservationState`: `operator_address` maps between Bech32 and 20 codec bytes; task identity, duty, versions, amount, status, and candidate slot retain their values. | `TaskLiabilityStoreState.operator_address` is raw bytes. Its existing task/duty/Bech32-operator key and derived indexes are unchanged. | `taskLiabilityToStore` and `ProjectTaskLiabilityStore` in `x/hub/keeper/responsibility_address_store.go` are the conversion points. Genesis imports private rows and `exportTaskLiabilities` returns public rows with key/value agreement. `TestModelProfileGenesisRebuildsCanonicalRegistration` checks raw storage and public Genesis export/import; the liability invariant tests retain deliberate corruption coverage. |
| `service_key_responsibility` | `hub.v1.ServiceKeyResponsibilityState`: `operator_address` maps between Bech32 and 20 codec bytes; participant type, responsibility ID/kind, string session/task IDs, height, and nonce retain their values. | `ServiceKeyResponsibilityStoreState.operator_address` is raw bytes. Its participant/Bech32-operator/Hash32 key and task index are unchanged. | `serviceKeyResponsibilityToStore` and `ProjectServiceKeyResponsibilityStore` in `x/hub/keeper/responsibility_address_store.go` are the conversion points. Genesis imports private rows and `exportServiceKeyResponsibilities` returns public rows with key/value agreement. `TestModelProfileGenesisRebuildsCanonicalRegistration` checks raw storage and public Genesis export/import; app invariants use the same projection. |
| `builder_fault` | `hub.v1.BuilderFaultState`: `builder_address` maps between Bech32 and 20 codec bytes; fault identity, kind, status, evidence, heights, and frozen slash basis points retain their values. | `BuilderFaultStoreState.builder_address` is raw bytes. Its Bech32/Hash32 key and prune index are unchanged. | `builderFaultToStore` and `ProjectBuilderFaultStore` in `x/hub/keeper/fault_address_store.go` are the conversion points. Genesis imports private rows and `exportBuilderFaults` returns public rows with key/value agreement. `TestBusObjectiveEvidenceResponsibilityGenesisConsistency` checks raw storage and public Genesis export/import; fault invariant tests retain deliberate corruption coverage. |
| `role_fault` | `hub.v1.RoleFaultState`: `operator_address` maps between Bech32 and 20 codec bytes; duty, classification, evidence, heights, and optional slash summary identity retain their values. | `RoleFaultStoreState.operator_address` is raw bytes; its optional slash summary ID preserves presence. Its Hash32 fault key and derived indexes are unchanged. | `roleFaultToStore` and `ProjectRoleFaultStore` in `x/hub/keeper/fault_address_store.go` are the conversion points. Genesis imports private rows and `exportRoleFaults` returns public rows with key/value agreement. `TestRoleFaultPruneIndexRebuildSurvivesRestart` checks raw storage and public Genesis export/import; fault invariant tests retain deliberate corruption coverage. |
| `slash_summary` | `hub.v1.SlashSummaryState`: `operator_address` maps between Bech32 and 20 codec bytes; source identity, optional task ID, custody waterfall, destination, and heights retain their values. | `SlashSummaryStoreState.operator_address` is raw bytes; seven public amount wrappers are flattened to atomic-unit strings and optional task ID presence is preserved. Its source-kind/Hash32/effect-index key is unchanged. | `slashSummaryToStore` and `ProjectSlashSummaryStore` in `x/hub/keeper/slash_summary_store.go` are the conversion points. Genesis imports private rows and `exportSlashSummaries` returns public rows with key/value agreement. `TestServiceSlashWaterfallMovesCustodyWritesSummaryAndTerminatesUnbonding` checks raw storage and public Genesis export/import; fault key tests retain deliberate corruption coverage. |
| `bridge_bootstrap` | `hub.v1.BridgeBootstrapState`: optional `recipient` and `fee_payer` map between Bech32 and 20 codec bytes; mode, binding hashes, amount, gas limit, and consumed height retain their values and presence. | `BridgeBootstrapStoreState` stores both addresses as optional raw bytes, flattens the optional amount to atomic units, and explicitly preserves every optional field. Its singleton key is unchanged. | `bridgeBootstrapToStore` and `ProjectBridgeBootstrapStore` in `x/hub/keeper/bridge_bootstrap_store.go` are the conversion points. Genesis imports private state and bridge export returns the public bootstrap. `TestBridgeBootstrapAddressesStoreRawBytesAndRoundTrip` checks raw storage and public Genesis export/import. |

The projections reject malformed addresses and invalid stored metadata. Callers assemble public responses from the projected message and do not persist a second public copy.

## Migration inventory

The current Hub and Task keeper collection declarations and the public descriptor yield 54 address-bearing value paths: 4 Hub and 1 Task path still use public value types, while the forty-nine collections above use private value projections. The count includes addresses reached through nested message fields. It does not include address-bearing store keys, events, or Query-only messages.

Remaining Hub paths: `Model`, `Profile`, `ModelSupport`, and `DailySupport`. Their public model/support schemas remain subject to later changes.

Remaining Task path: `TaskCore`. Its public `model_id` contract may change, so the private value schema waits for that decision rather than being built twice.

Model and support paths wait for their public schema changes to settle. Each batch must keep its public projection and Genesis round trip together; focused tests run per batch and the full suite runs at the end.
